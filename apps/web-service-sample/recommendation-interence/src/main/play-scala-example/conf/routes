# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# OfficeDepot healthcheck
GET     /                      controllers.HomeController.index

# OfficeDepot healthcheck
GET     /health                controllers.HealthCheckController.index

# Run subscription model
POST    /recModel     controllers.ModelController.recModel

# OfficeDepot model versionCheck
GET     /version               controllers.VersionCheckController.index
